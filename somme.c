#include <stdio.h>

typedef enum {consomateur,station} type_Utilisateur;

int main(){
    Donnee_station donne;
    type_Utilisateur d = station;

    while(scanf("%d;%u;%u;%u", &d, &donne->ID_station, &donne->consomation, &donne->capacite) == 4){
        if(d==station){
            avl = insertionAVL(avl,donne);
        }
        else if(d==consomateur){
            recherché valeur pluis additionné conso ensemble
        }
    }
    return 0;    
}